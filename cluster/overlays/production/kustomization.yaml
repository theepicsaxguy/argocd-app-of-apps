apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
resources:
  - ../../base/k3s-server.yaml
  - ../../base/loadbalancer.yaml
# (Note: The Metallb configuration now replaces the separate vip.yaml.)
# Optionally add environment‑specific patches here.
