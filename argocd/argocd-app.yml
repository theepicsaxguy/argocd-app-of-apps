apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: manage-argocd
  namespace: argocd
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  source:
    repoURL: 'https://github.com/xxx/homelab.git'
    path: namespaces
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
